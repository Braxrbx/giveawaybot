<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
</head>
<body class="bg-gray-900 text-white min-h-screen flex items-center justify-center">
    <div class="bg-gray-800 p-8 rounded-lg shadow-xl max-w-md w-full">
        <div class="text-center">
            <% if (locals.error) { %>
                <div class="mb-6 p-4 rounded-lg <%= error === 'role' ? 'bg-yellow-900' : 'bg-red-900' %>">
                    <% if (error === 'role') { %>
                        <p class="text-yellow-200">You need the Staff role to access the dashboard.</p>
                    <% } else if (error === 'config') { %>
                        <p class="text-red-200">Server configuration error. Please contact an administrator.</p>
                    <% } else if (error === 'server') { %>
                        <p class="text-red-200">An error occurred. Please try again.</p>
                    <% } %>
                </div>
            <% } %>

            <h1 class="text-3xl font-bold mb-6">Staff Dashboard</h1>
            
            <% if (locals.user) { %>
                <div class="mb-6">
                    <img src="https://cdn.discordapp.com/avatars/<%= user.id %>/<%= user.avatar %>.png" 
                         alt="Profile" 
                         class="w-24 h-24 rounded-full mx-auto mb-4">
                    <p class="text-xl"><%= user.username %></p>
                </div>
                <div class="space-y-4">
                    <a href="/dashboard" class="block w-full bg-blue-600 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded transition">
                        Go to Dashboard
                    </a>
                    <a href="/auth/logout" class="block w-full bg-red-600 hover:bg-red-700 text-white font-bold py-2 px-4 rounded transition">
                        Logout
                    </a>
                </div>
            <% } else { %>
                <a href="/auth/discord" class="block w-full bg-indigo-600 hover:bg-indigo-700 text-white font-bold py-2 px-4 rounded transition">
                    Login with Discord
                </a>
            <% } %>
        </div>
    </div>
</body>
</html> 